apiVersion: v1
kind: Service
metadata:
  name: {{ include "opencost-cloudcost-exporter.fullname" . }}
  labels:
    {{- include "opencost-cloudcost-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "opencost-cloudcost-exporter.selectorLabels" . | nindent 4 }}
